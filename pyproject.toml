[project]
name = "streamlit-projects"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "krish14-ai", email = "kkhandelwal292@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "streamlit>=1.62.0",
]

[project.scripts]
streamlit-projects = "streamlit_projects:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
